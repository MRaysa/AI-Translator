# Copy this file to `.dev.vars` and fill in your real key.
# `.dev.vars` is git-ignored and used only by `wrangler dev` for local development.
# In production, set the key with: npx wrangler secret put OPENAI_API_KEY

OPENAI_API_KEY=sk-your-openai-key-here
