# Screenshots & demo

Drop capture files here so they render in the main README:

| File        | Suggested capture                                    |
| ----------- | ---------------------------------------------------- |
| `home.png`  | The landing page (hero + translator)                 |
| `tools.png` | A translation with the AI tools / a tool result open |
| `lingo.png` | The floating "Lingo" assistant open                  |
| `demo.gif`  | A 20–30s screen recording of a full flow             |

**Tips**

- PNG at ~1600px wide looks crisp on GitHub.
- For the GIF: record with ScreenToGif / LICEcap, keep it under ~10 MB.
- Show: type text → translate → switch mode → run an AI tool → open chat.
